import 'package:flutter/material.dart';

class TareasInitialView extends StatelessWidget {
  const TareasInitialView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Lista de tareas"));
  }
}