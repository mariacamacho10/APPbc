import 'package:flutter/material.dart';

class TareasTerminadasInitialView extends StatelessWidget {
  const TareasTerminadasInitialView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Lista de tareas terminadas"));
  }
}